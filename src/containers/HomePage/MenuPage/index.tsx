import React from 'react';

interface MenuScreenProps {

};

function MenuScreen({}: MenuScreenProps) {
    return (
        <div className={"w-full h-full flex justify-center items-center"}>
            Menu Screen
        </div>
    );
}

export default MenuScreen;